# Vulkan-Hpp headers

Vendored from KhronosGroup/Vulkan-Hpp tag `v1.3.275` to match Android NDK 27
Vulkan header version `275`.

These headers are used only for the Android llama.cpp Vulkan backend build. The
upstream `llama.cpp` submodule remains unmodified.
