# Transforms

Transforms convert reviewed source snapshots into the ChromaLab Knowledge Pack schema.

Phase 6C intentionally leaves external transforms empty. Add a transform only after:

1. source license is documented in `sources.yaml`;
2. attribution requirements are known;
3. redistribution/bundling is permitted;
4. tests prove the transform cannot introduce measured chromatographic metrics as reference knowledge.
