# Scientific Review Protocol

For chromatographic/scientific outputs:
- values must be labeled by provenance;
- invalid calibration blocks release-quality metrics;
- inferred values must be marked;
- user-confirmed/manual values must be marked;
- VLM cannot produce numeric metrics;
- report caveats must be precise and visible.
